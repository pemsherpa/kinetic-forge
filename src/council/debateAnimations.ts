export const DEBATE_TIMINGS = {
  COUNCIL_START_DELAY: 200,          // ms before THINKING shimmer starts
  ROUND_DELAY: 1500,                 // ms between rounds
  TYPEWRITER_SPEED_MS: 20,           // ms per character for log streaming
  CONSENSUS_VOTE_INTERVAL: 300,      // ms between simulated votes coming in
  CONSENSUS_POP_DELAY: 400,          // delay before ConsensusNode pop-in
  PHASE2_FADE_IN_DELAY: 700,         // ms after consensus before Phase 2 renders
  PIPELINE_STAGGER: 0.06             // s stagger for pipeline node cascade
};
